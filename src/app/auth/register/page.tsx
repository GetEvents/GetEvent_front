import RegisterForm from "@/components/form/RegisterForm/";

const Register = async () => {
  return (
    <div className="w-full">
      <RegisterForm id={false} />
    </div>
  );
};

export default Register;
